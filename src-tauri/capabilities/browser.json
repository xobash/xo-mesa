{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "browser",
  "description": "Minimal lifecycle capability for remote browser child webviews.",
  "windows": ["browser-*"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-set-focus"
  ]
}
